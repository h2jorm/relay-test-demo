const assert = require('assert')
const Atom = require('../lib/atom')

describe('Class Atom', () => {
  it('should pass', () => {
    assert.ok(true)
  })
  it('should fail', () => {
    assert.fail(true)
  })
})
